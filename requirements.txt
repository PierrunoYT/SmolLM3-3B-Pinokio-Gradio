# SmolLM3-3B Chatbot Requirements
# Note: PyTorch installation is handled by torch.js based on platform/GPU,
# so torch is intentionally not pinned here.

# Core ML dependencies
transformers>=4.53.0
accelerate
tokenizers
safetensors

# Web interface
gradio>=4.0.0

# Text processing and model support
sentencepiece
protobuf
huggingface-hub

# Utility libraries
numpy
packaging
pyyaml
regex
requests
tqdm
